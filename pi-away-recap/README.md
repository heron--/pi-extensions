# away-recap

A small [pi](https://github.com/earendil-works/pi-coding-agent) extension that
catches you up when you sit back down. Step away, come back, and the first key
you press brings up two or three sentences on what happened while you were
gone — written by whichever cheap model is next in the rotation.

```text
 index.ts in pi-away-recap is 343 lines long, and it's the only .ts file there.
  generated by DeepSeek V4 Flash 0731 (Baseten)
```

It renders as a widget directly above the prompt, and clears as soon as you
send anything, because by then it is describing a gap you have already crossed.

## What counts as "away"

No keystrokes for five minutes, by default. That is a proxy, and worth being
explicit about: pi does not hand extensions the terminal's focus events, so an
extension cannot tell "walked away" from "sat and read the whole run". The only
presence signal available is input to the editor, so a long run you watched
end-to-end and then typed after will still produce a recap.

The recap is skipped when:

- the agent is still working — `ctx.isIdle()` is false, so the screen is
  already telling you what is happening
- the prompt already has a draft in it, which means you never really left
- nothing was added to the session while you were gone
- a recap is already being generated

## The rotation

Each recap uses the next model in this order, and says which one wrote it:

1. DeepSeek V4 Flash
2. GLM 5.3 Flash
3. Gemini 3.8 Flash
4. GPT-5.6 Luna
5. Claude Haiku 4.5

Models are matched against the catalogue by id pattern rather than by
`provider/id`, so a model that arrives under a different provider name still
resolves, and an entry that resolves to nothing — or that has no configured
auth — drops out of the rotation quietly instead of breaking it. The call is
made through `ctx.modelRegistry.complete()`, which resolves provider auth
itself.

Nothing about a recap is load-bearing, so failure is silent: a gateway that is
down, unauthenticated, or slower than the 30s timeout costs the session
nothing and leaves no widget. Use `/away-recap now` to see the error.

## What the model is shown

Not the raw transcript. Tool arguments and results are the bulk of a session
and almost none of what a recap needs, so each entry added while you were away
collapses to one line — what the agent said, which tools it ran, which failed,
whether the context was compacted. The digest is capped at 6,000 characters,
keeping the tail, since the newest activity matters most.

## Usage

```text
/away-recap                  toggle
/away-recap on               enable
/away-recap off              disable
/away-recap now              recap the last threshold's worth of activity
/away-recap after 15         set the away threshold, in minutes
/away-recap models           show the rotation and which model is next
```

`after` accepts fractions, which is also how to exercise the automatic path
without waiting five minutes: `/away-recap after 0.1` triggers on the first
keystroke after six seconds of quiet.

## Development

```bash
npm run typecheck
pi
```

Run `node scripts/link-extensions.mjs --yes` from the repository root after a
fresh checkout.
