node_modules/
package-lock.json

# Generated by scripts/sync-pi-types.mjs — machine-specific absolute paths to
# the local pi install, so it must not be committed. Recreated by
# `npm install`, `npm run typecheck`, or `npm run sync-types`.
tsconfig.paths.json

# Which extensions are live in THIS checkout. Symlinks here are machine-local
# and change constantly while developing — add/remove freely without it showing
# up in git status. See README "Loading extensions while developing".
.pi/

.DS_Store
*.tsbuildinfo
*.log
